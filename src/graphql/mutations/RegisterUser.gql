mutation RegisterUser($username: String!, $email: String!, $password: String!, $recaptchaResponse: String!) {
    registerUser(input: { username: $username, email: $email, password: $password, recaptchaResponse: $recaptchaResponse}) {
        user {
            id
        }
    }
}
