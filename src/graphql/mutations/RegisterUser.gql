mutation RegisterUser($firstName: String, $lastName: String, $email: String!, $password: String!, $recaptchaResponse: String!) {
    registerUser(input: { firstName: $firstName, lastName: $lastName, email: $email, password: $password, recaptchaResponse: $recaptchaResponse}) {
        user {
            id
        }
    }
}
